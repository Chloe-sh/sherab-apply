# 약사여래 수행 신청 페이지 — 인수인계서

> ⚠️ 이 파일은 공개 저장소용입니다. 개인정보(계정 이메일·연락처·계좌번호)는 가려져 있으며, 실제 값은 구글 시트 또는 로컬 원본(`D:\클로드\sherab-retreat\인수인계서.md`)에서 확인하세요.


작성일: 2026-08-05 (23:30 기준) · **갱신: 2026-08-09** (08-08~09 변경분 반영 — 0·2·3·4·5·7장)
작성자: 이전 세션 Claude → 다음 담당자(사람/AI)에게

---

## 0. 30초 요약

- **신청 페이지는 이미 실서비스 중**입니다. 링크: **https://chloe-sh.github.io/sherab-apply/**
- 화면(프런트)은 **GitHub Pages**, 신청 접수 저장(DB)은 **구글 시트 + Apps Script API**로 분리되어 있습니다.
- **(2026-08-08 변경, 커밋 `eb8dbaf`)** 화면에 표시되는 문구·가격·질문·계좌는 이제 **`index.html` 안의 `CONFIG_DATA`가 정본**입니다. 시트를 고쳐도 화면은 바뀌지 않습니다 — `CONFIG_DATA` 수정 → git push로 반영하세요. 시트는 **신청 접수 저장 전용**입니다.
- 페이지 디자인/구조를 바꾸려면 `index.html` 수정 → **git push** (1분 내 자동 반영).
- 일정표는 텍스트 리스트로 구현 완료(이미지 불필요). 남은 미완료는 아래 5장 참고(보안 패치가 최우선).

---

## 1. 접속 정보 / 자산 목록

| 항목 | 값 |
|---|---|
| **신청 링크(참가자용)** | https://chloe-sh.github.io/sherab-apply/ |
| 개인 초대 링크 | 위 주소 + `?to=이름` (예: `...?to=김영희`) |
| **통합 관리 시트(마스터)** | 구글 드라이브에서 `2026 약사여래 수행 통합 관리` 검색 (주소는 이 저장소가 public이라 비공개 — 별도 전달) |
| Apps Script 프로젝트 | 위 시트 → 확장 프로그램 → Apps Script |
| Apps Script API 주소 | `index.html` 상단 `API_URL` 변수 참조 (배포 URL이라 공개되어도 무방) |
| GitHub 저장소 | https://github.com/Chloe-sh/sherab-apply (public) |
| 구글 계정 | (시트 소유 계정 — 사용자에게 문의) |
| 로컬 작업 폴더 | `D:\클로드\sherab-retreat\` (원본) |
| 로컬 배포 폴더 | `D:\클로드\sherab-apply\` (git 저장소, index.html + images) |

---

## 2. 시스템 구조 (중요 — 이것만 이해하면 됨)

```
[참가자]
   │  https://chloe-sh.github.io/sherab-apply/  ← GitHub Pages (정적 HTML 1개)
   ▼
index.html ── 설정·프로그램·질문: 내장 CONFIG_DATA 사용 (2026-08-08부터 시트 로드 없음)
           └─ 신청 제출 시:  POST .../exec               → 신청 저장
   ▼
[Apps Script (Code.gs)] = API 전용. 화면은 그리지 않음.
   ▼
[통합 시트] 접수원본(백업) + [사업장 시트 C/D/E] 신청내역
   ▼
[통합 시트] 개별내역(IMPORTRANGE 실시간) → 통합내역(사람별 자동집계)
```

### 왜 이 구조인가 (반드시 유지할 것)
초기에는 Apps Script가 화면까지 그렸는데(HtmlService), 이 방식은 **googleusercontent 샌드박스 iframe**을 쓰기 때문에 **엣지 브라우저, 카카오톡 인앱 브라우저, 서드파티 쿠키 차단 환경에서 페이지가 안 열립니다.** 실제 사용자 2명이 접속 실패했습니다.
→ 화면을 GitHub Pages로 분리해서 해결했습니다. **다시 HtmlService로 되돌리지 마세요.**

---

## 3. 수정 방법 (2가지 경로)

### 경로 A. 시트에서 수정 — ⚠️ 2026-08-08부터 화면에는 반영 안 됨
설정·프로그램·질문 값이 `index.html`의 `CONFIG_DATA`로 내장되면서(커밋 `eb8dbaf`), **시트를 고쳐도 화면 문구는 바뀌지 않습니다.** 화면 수정은 경로 B를 쓰세요. 아래 표는 `CONFIG_DATA`의 키 구조 참고용으로 남겨둡니다. 단, **프로그램 코드 체계(G/C/D/E/F)는 신청 저장의 기준이므로 시트 프로그램 탭과 계속 맞춰야 합니다.** 신청 데이터 탭(개별내역·통합내역·접수원본)은 그대로 유효합니다.

| 탭 | 무엇을 바꾸나 |
|---|---|
| **설정** | 사이트제목, 게이트제목/문구/버튼, 쉐랍 소개, 입금안내, 문의처, 환불규정, 완료메시지, 각종 이미지URL |
| **프로그램** | 코드(A~E), 프로그램명, **사용여부(Y/N)**, 참가비, 일정, 장소, 간단설명, 이미지URL, **J열=입금계좌(예금주)** |
| **질문** | 프로그램별 신청서 질문 (코드/순서/질문/유형/선택지/필수/보조설명) |
| 개별내역·통합내역·연결확인·접수원본 | **읽기 전용. 수정 금지** |

- `설정` 탭에 다음 항목을 **추가**하면 자동 인식됩니다(현재 없음, 필요 시 행 추가):
  - `일정표이미지URL` — 2번 일정표 섹션에 이미지 표시
  - `초대글제목`, `초대글` — 초대장 안의 글 교체 (빈 줄로 문단 구분)

⚠️ **시트 편집 시 주의**: 구글 시트에 붙여넣기할 때 클립보드에 **이미지**가 들어있으면 셀이 아니라 시트에 이미지 개체가 박힙니다. 이 사고가 실제로 1회 발생했고 제거했습니다. 텍스트 붙여넣기는 `Ctrl+Shift+V`(서식 없이) 사용 권장.

### 경로 B. 페이지 코드 수정 (디자인·구조 변경)
```bash
# 1) 원본 수정
D:\클로드\sherab-retreat\index.html

# 2) 배포 폴더로 복사 후 push (아래 명령 한 줄)
```
```bash
Copy-Item "D:\클로드\sherab-retreat\index.html" "D:\클로드\sherab-apply\index.html" -Force; Set-Location "D:\클로드\sherab-apply"; git add -A; git commit -m "수정내용"; git push
```
- 반영까지 **12~72초** 소요. 확인은 `Invoke-WebRequest`로 특정 문자열 존재 여부 폴링 권장.
- 브라우저 캐시 때문에 안 바뀌어 보이면 `?v=아무값` 붙여서 접속.

### Code.gs(백엔드) 수정 시
1. 시트 → 확장 프로그램 → Apps Script → `Code.gs` 전체 교체 → Ctrl+S
2. **Deploy → Manage deployments → 연필(수정) → Version: New version → Deploy**
   ⚠️ "New deployment"를 누르면 **URL이 바뀌어** 기존 링크가 죽습니다. 반드시 Manage deployments.
3. `index.html`의 `API_URL` 상수는 이 URL이 하드코딩되어 있음.

---

## 4. 현재 페이지 구성 (2026-08-05 기준)

### 게이트 화면
- 배경 `#04194A` + 미색 `#FBF8F2` 텍스트
- 혜봉 선생님·쉐랍 텐진 선생님 사진(`images/gate.jpg`)
- 티베트어 인사 `བཀྲ་ཤིས་བདེ་ལེགས།` (따시델렉)
- 문구: "2026 쉐랍 텐진의 약사여래 체험 수행" (2026-08-09 '~과 함께하는'에서 변경, og:description도 동일)
- 버튼: "약사여래 체험수행 로드맵 보기"
- 하단: 주최/주관 2줄 (볼드 처리)

### 신청 페이지 (배경 `#E8EFFE`, 폰트 Pretendard ExtraLight)
| # | 섹션 | 비고 (2026-08-09 기준) |
|---|---|---|
| — | 초대장 | 네이비 박스 안에 INVITATION + 초대장 + 초대 글(좌우 맞춤) + 합장 아기 사진(투명 배경) |
| 1 | 혜봉 선생님 | 소개 |
| 2 | 쉐랍 텐진 | 소개글 + 사진 |
| 3 | **일정표** | 제목 "2026 쉐랍 텐진 방한 일정표". 월별 텍스트 리스트(하드코딩, 이미지 불필요). 세부 일정 표는 08-09에 수행 선택 카드로 이동 |
| 4 | 수행 사전 | 08-09에 '수행 안내'에서 개칭(배너 h3도 '약사여래 수행 사전'). 배너 클릭 시 상세 설명 |
| 5 | 수행 선택 | G/C/D/E/F 5개 카드. 제목은 남색 배너+흰 글씨. 일정 미니카드 아래 '일정 자세히 보기 ▾' 아웃라인 버튼 → 카드 안에서 세부 일정 표 펼침(코드 매핑 `DS_MAP`: G→ds1, C→ds2, D→ds4, F→dsDetail. E는 세부 일정 없음). **신청 토글은 하단 '이 수행을 신청합니다' 체크 줄에서만** — 카드 본문 클릭으로는 선택 안 됨 |
| 6 | 신청자 기본 정보 | 수행 경험 유/무 + '유' 선택 시에만 활성화되는 하위옵션(쉐랍 텐진 수련 경험). 휴대폰은 010 고정 + 뒤 8자리 입력 |
| 7 | 선택한 수행별 신청서 | CONFIG_DATA의 questions 정의대로 자동 생성 |
| 8 | 참가비 안내 | 참가비표 + **선택 기관별 입금계좌 표**(각진 스타일) |
| 9 | 동의 및 기타 | 개인정보(필수)·기록물 동의 |

### 완료 화면
배경 `#E8EFFE` + `#04194A` 텍스트, 접수번호·신청내역·**기관별 입금계좌 표**

### 디자인 토큰
- 네이비 `#04194A` / 미색 `#FBF8F2` / 본문배경 `#E8EFFE` / 테두리 `#D5DEEF` / 본문글자 `#111`
- 폰트: Pretendard Variable ExtraLight(200) — CDN jsdelivr, 티베트어만 Noto Serif Tibetan
- **골드 계열은 전부 제거됨**(수행안내 상세 제목 `#A8862D`만 예외)

### 개발용 편의 파라미터
- `?view=main` → 신청 화면 바로 열기
- `?view=done` → 완료 화면 바로 열기 (더미 데이터)
- `D:\클로드\sherab-retreat\3페이지-미리보기.html` → 3화면 나란히 보기 (라이브 iframe)

---

## 5. ⚠️ 미완료 / 인계 사항

### 5-1. ~~일정표 이미지~~ → 해결됨 (2026-08-08~09)
- 일정표는 텍스트 리스트(월별 그룹)로 하드코딩 구현 완료. 이미지 불필요.
- ⚠️ 주의: `CONFIG_DATA` settings에 `일정표이미지URL` 값을 넣으면 `scheduleBody`가 통째로 이미지로 **교체**되어 텍스트 일정표가 사라짐. 넣지 말 것.

### 5-2. 사업장 시트 담당자 공유 (사용자 액션 필요)
- `프로그램` 탭 I열의 사업장시트URL을 각 담당자에게 **편집자**로 공유해야 함. 아직 미공유로 추정.
- 담당자용 안내는 `관리자-사용법.md` 1장만 전달하면 됨.

### 5-3. 계좌 정보 미확정
- C(행복수업), E(혜봉추모기념사업회)는 `계좌 추후 안내`로 들어가 있음. 확정되면 `프로그램` 탭 J열만 수정.
- D(아나빠나사띠명상요가)만 실제 계좌 입력됨: (계좌번호는 시트 프로그램 탭 J열 참고)

### 5-4. 일정·장소 placeholder
- C, E의 일정/장소가 "(일정 확정 시 수정)", "(장소 추후 안내)" 상태.

### 5-5. 테스트 데이터 3건
- 접수원본/사업장 시트에 `테스트`(A), `테스트둘`(B), `테스트셋`(E) 존재. 지워도 되고 담당자 연습용으로 둬도 됨.
- A·B는 현재 사용여부 N이라 실제 신청과 섞이지 않음.

### 5-6. 🔴 Code.gs 보안 패치 미적용 (최우선)
- 2026-08-08 보안 점검 결과, **스프레드시트 수식 인젝션** 취약점이 확인됨. 신청자가 이름·건강상태 칸에 `=IMPORTXML(...)` 을 넣으면 담당자가 시트를 여는 순간 실행되어 옆 셀의 신청자 개인정보가 외부로 전송될 수 있음.
- 페이지(`index.html`) 쪽 대응은 완료: 민감정보 별도 동의, 입력 길이 제한, 허니팟, CSP, CDN 무결성 검증.
- **남은 작업 — Apps Script 쪽**: [`보안패치-Code.gs.md`](보안패치-Code.gs.md) 를 열어 STEP 1~5를 그대로 따라 하면 됨. 붙여넣기 + 재배포로 끝남.
- 같이 지켜야 할 것: 마스터 시트·사업장 시트는 **링크 공유 금지, 이메일 지정 공유만**. 저장소가 public이라 링크 공유로 바꾸는 순간 명단이 노출됨.

---

## 6. 알려진 함정 (같은 실수 반복 방지)

1. **`appendRow` 금지** — 사업장 시트에 입금확인 체크박스가 1000행까지 깔려 있어서 `appendRow`를 쓰면 1001행에 기록됩니다. 반드시 A열 첫 빈 행을 찾아 `setValues`. (`Code.gs`의 `submitApplication` 참고, 복구 함수 `repairVenueRows()`도 있음)

2. **`applyContentUpdates()` 함수는 작동하지 않았음** — Code.gs에 시트 일괄 수정용으로 넣어뒀으나, 실행은 "완료"되는데 시트에 반영되지 않는 현상 발생(원인 미규명). **시트 값은 손으로 고치는 것을 권장.** 이 함수를 신뢰하지 마세요.

3. **`연결확인` 탭의 IMPORTRANGE는 일부러 IFERROR로 감싸지 않음** — 감싸면 "액세스 허용" 버튼이 안 뜹니다.

4. **개별내역(IMPORTRANGE)은 수 분 지연** 가능. 접수원본(숨김)에는 즉시 기록되므로 데이터 유실 아님.

5. **한글 `type` 입력 시 첫 단어 누락** — 구글 시트에 computer type으로 한글 입력하면 첫 어절이 씹히는 경우 있음. 클립보드 `Ctrl+V` 사용 권장 (단, 직전에 반드시 `Set-Clipboard` 재실행 — 클립보드가 비거나 이미지로 덮이는 일이 잦음).

6. **URL 단축은 이제 불필요** — GitHub Pages 주소가 짧아서 카톡에서 잘리지 않습니다. 예전 tinyurl.com/275h6xdr는 영어 중간 페이지가 떠서 사용 중단.

7. **CSP(보안 정책) 때문에 외부 스크립트가 차단됨** — `index.html` 3번째 줄의 `Content-Security-Policy` meta 태그가 허용된 도메인 외의 리소스를 막습니다. 새 CDN 라이브러리나 외부 폰트를 추가했는데 로딩되지 않으면, 브라우저 콘솔(F12)에 `Refused to load...` 가 찍혔는지 보고 해당 도메인을 이 meta 태그에 추가하세요. 이미지는 `https:` 전체가 허용되어 있어 시트 `설정` 탭의 이미지 URL은 그대로 동작합니다.

8. **CDN 버전을 올리면 `integrity` 해시도 바꿔야 함** — Pretendard 폰트 CSS에 무결성 검증(SRI)이 걸려 있습니다. `@v1.3.9` 를 다른 버전으로 바꾸면 해시가 맞지 않아 폰트가 통째로 로딩되지 않습니다. 버전을 올릴 거면 `integrity` 속성을 지우거나 새 해시로 교체하세요.

---

## 7. 최근 작업 이력 (2026-08-05 하루)

1. 구글 폼 5종에서 정보 추출 → 페이지 최초 제작
2. Apps Script 배포 → 사업장별 DB 분리(C/D/E 시트 자동 생성) → E2E 테스트
3. 브라우저 호환성 문제 발견 → **GitHub Pages로 프런트 이관** (가장 중요한 변경)
4. 디자인 개편: 네이비/미색 컬러, Pretendard, 게이트 사진, 초대장 통합, 아기 사진(누끼)
5. 섹션 추가: 일정표(빈 자리), 수행 안내 8배너 + 오버레이 상세
6. 표기 정리: 혜봉 스님→선생님, '닥터' 제거, 꿈속→꿈 속, 챠크라→차크라, 아나빠나사띠→아나빠나사띠명상요가
7. 기능 추가: 수행경험 하위옵션(유 선택 시만 활성), 기관별 입금계좌 표
8. 문의처 변경: (시트 설정 탭 문의처 참고)

**(구) 마지막 배포 상태**: GitHub Pages `a54247b` 커밋 반영 완료 / Apps Script Version 4 배포 완료 / 시트 값 최신 (API로 검증 완료)

---

## 7-2. 최근 작업 이력 (2026-08-08~09 세션)

이전 세션(08-08, 커밋 `4bd2a2e`~`ddd0898`): og 미리보기 메타태그, 룽즁쥭 하이라이트 정리, 수행 선택 재구성(G 룽즁쥭 자율보시 분리·C 꿈요가 개칭, 카드 제목 '수행명 | 기관명'), **구글 시트 텍스트 로드 제거(`eb8dbaf`) — CONFIG_DATA 정본화**, 휴대폰 010 고정 입력.

이번 세션(08-08 밤~09):

1. `dc7b99a` — 일정표 제목 "2026 쉐랍 텐진 방한 일정표"로 변경. 일정표 섹션의 '세부 일정 보기' 버튼 제거, 세부 일정 표(ds1/ds2/ds4/dsDetail)를 각 수행 선택 카드 안으로 이동(`renderPrograms()`의 `DS_MAP` 참고 — 정적 dsBlock을 카드 안으로 `insertAdjacentElement`로 옮기는 방식). 카드 아무 곳 클릭 시 신청되던 동작 제거 — 체크 줄에서만 토글. 수행 사전 배너 '약사여래 수행 안내'→'약사여래 수행 사전'.
2. `65ee99e` — 카드 제목 남색 배너+흰 글씨(스크린샷 시안 반영), '일정 자세히 보기'를 밑줄 링크에서 전폭 아웃라인 버튼으로(잘 안 보인다는 피드백), 펼침 라벨 '접기 ▴'.
3. `41ca12c` — 게이트 문구 "2026 쉐랍 텐진의 약사여래 체험 수행"(og:description 포함).
4. 배포 검증: curl로 Pages 반영 확인 완료. "커밋이 안 된다"는 문의가 있었으나 실제로는 전부 반영됨 — **브라우저 캐시** 문제였음(Ctrl+F5 안내).

### 구현 주의 (이번 변경분)
- `render()`/`renderPrograms()`는 **한 번만 실행**되는 전제. 만약 재호출 구조로 바꾸면 `progList.innerHTML = ''` 시점에 카드 안으로 옮겨진 dsBlock이 함께 파괴되어 세부 일정이 사라짐 — 재호출하려면 dsBlock을 먼저 밖으로 대피시켜야 함.
- 세부 일정 내용 자체는 여전히 index.html의 정적 HTML(구 일정표 섹션에서 이동한 블록). 시간표 수정은 해당 dsBlock HTML을 직접 편집.

---

## 8. 작업 스타일 메모

- 한국어로 소통. 캐주얼한 반말체 요청이 많음.
- **스크린샷에 빨간 펜으로 표시**해서 수정 요청하는 방식을 자주 씀 → 이미지를 잘 읽고 정확한 위치를 파악할 것.
- 이미지는 **클립보드 복사**로 전달함 → `[System.Windows.Forms.Clipboard]::GetImage()`로 받기.
- 누끼(배경 제거)는 `rembg` 사용 (설치됨). **"얼굴은 절대 바꾸지 마"** 요구가 반복됨 → 생성형 편집 절대 금지, 배경 알파만 제거할 것.
- 수정 요청을 여러 개 한 번에 보내고, 작업 중에도 계속 추가 요청을 보냄 → 놓치지 말고 목록화할 것.
- 결과 확인을 좋아함 → 배포 후 실제 페이지 스크린샷으로 검증해서 보여줄 것.

---

## 9. 다음 담당자가 바로 할 일 (체크리스트)

- [x] ~~일정표 이미지 받아서 2번 섹션에 넣기~~ → 텍스트 일정표로 구현 완료 (08-08~09)
- [ ] 🔴 `보안패치-Code.gs.md` STEP 1~5 적용 (5-6장, 최우선)
- [ ] G·C 입금계좌 확정되면 **`CONFIG_DATA`의 account 값** 수정 후 push (시트 J열 수정만으로는 화면에 안 나옴)
- [x] ~~C·E 일정/장소 확정되면 수정~~ → 전 프로그램 일정·장소 입력 완료 (CONFIG_DATA 기준)
- [ ] 사업장 시트를 각 담당자에게 공유 (편집자 권한)
- [ ] 테스트 데이터 3건 삭제 여부 결정
- [ ] (선택) 초대장 디자인 이미지로 교체 시 `CONFIG_DATA` settings의 `초대장이미지URL` 값 사용 (시트 아님)
