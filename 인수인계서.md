# 약사여래 수행 신청 페이지 — 인수인계서

> ⚠️ 이 파일은 공개 저장소용입니다. 개인정보(계정 이메일·연락처·계좌번호)는 가려져 있으며, 실제 값은 구글 시트 또는 페이지 코드에서 확인하세요.


작성일: 2026-08-05 · **갱신: 2026-08-10 새벽** (08-09 밤 ~ 08-10: 카드 단순화 · 전 수행 소개 화면 · 신청 흐름 개편 · 참가비 산출 내역 — `64b5e84`까지 전부 라이브)
작성자: 이전 세션 Claude → 다음 담당자(사람/AI)에게

---

## 0. 30초 요약

- **신청 페이지는 실서비스 중**: **https://chloe-sh.github.io/sherab-apply/**
- 화면(프런트)은 **GitHub Pages**, 신청 저장(DB)은 **구글 시트 + Apps Script API**.
- 화면 문구·가격·질문·계좌는 **`index.html` 안의 `CONFIG_DATA`가 정본**. 시트는 접수 저장 전용.
- **(2026-08-09 대개편)** 이제 단일 스크롤 페이지가 아니라 **허브(메뉴) 구조**입니다:
  게이트 → **허브** → { 스승님 소개·수행 사전 / 일정표 / 수행 신청 } + **하단 고정 메뉴**(모바일) / 상단 바(PC).
- **(08-09 밤~08-10) 신청 화면 재편**: 카드는 요약 4행+자세히 보기(전 수행 소개 화면), 체크하면 바로 신청서 작성으로. 2번 카드 = 칩 배너 → 수행별 신청서(최근 선택 우선) → 공통 기본 정보. 행복수업 계좌 확정(카카오뱅크). 상세는 2-2·4장.
- ⚠️ **수정은 `D:\클로드\sherab-apply\index.html`에 직접** 하세요. (아래 3장 — 예전 문서의 "retreat에서 복사" 경로는 폐기)
- **Code.gs 보안 패치는 배포 완료(v5)** — 단, 정본은 Apps Script **편집기**이고 로컬 `Code.gs` 파일은 패치 이전 구본. **로컬 파일을 편집기에 통붙이면 패치가 유실**됩니다 (5-1장).

---

## 1. 접속 정보 / 자산 목록

| 항목 | 값 |
|---|---|
| **신청 링크(참가자용)** | https://chloe-sh.github.io/sherab-apply/ |
| 개인 초대 링크 | 위 주소 + `?to=이름` |
| 미리보기 파라미터 | `?view=hub` `?view=dict` `?view=sched` `?view=main` `?view=done` |
| **통합 관리 시트(마스터)** | 구글 드라이브에서 `약사여래 수행 신청 관리(통합)` 검색 (주소는 public 저장소라 비공개) |
| Apps Script API | `index.html`의 `API_URL` 상수 (배포 URL, 공개 무방) |
| GitHub 저장소 | https://github.com/Chloe-sh/sherab-apply (public) |
| **작업 폴더(정본)** | `D:\클로드\sherab-apply\` — git 저장소, 여기서 직접 수정 |
| (구) 원본 폴더 | `D:\클로드\sherab-retreat\` — **2026-08-07 이후 구버전. 여기서 복사하지 말 것** |
| 로컬 미리보기 | `.claude/launch.json`의 `sherab-apply` (python http.server, 포트 8734) |
| 팀 구글폼(약사여래 안거 원문) | forms.gle/2cQBQvWrUGkhM6K6A — 소개글·모집요강의 출처 |

---

## 2. 시스템 구조

```
[참가자] → GitHub Pages (index.html 1장, CONFIG_DATA 내장)
   └ 신청 제출 시 POST API_URL → [Apps Script Code.gs] → [통합 시트 접수원본 + 사업장 시트 C/D/E]
                                    → 통합 시트 개별내역(IMPORTRANGE) → 통합내역(자동집계)
```

- HtmlService로 되돌리지 말 것 (엣지·카톡 인앱에서 안 열림 — 실사용자 접속 실패 이력 있음).
- 프로그램 코드 체계(G/C/D/E/F)는 신청 저장 기준이므로 **시트 프로그램 탭과 CONFIG_DATA를 계속 맞출 것**.

### 2-2. 화면(뷰) 구조 — 2026-08-09 대개편

한 HTML 안에서 `showView(id)`로 화면을 전환하는 SPA 구조:

| 뷰 id | 내용 | 하단/상단 메뉴 |
|---|---|---|
| `gate` | 게이트(영상+따시델렉) | 숨김 |
| `hub` | 메인 메뉴: 사전 카드(좌)·일정표 카드(우)·수행 신청 배너 | 표시 (홈 활성) |
| `pageDict` | 스승님 소개(혜봉·쉐랍) + 수행 사전 8배너 | 표시 |
| `pageSched` | 방한 일정표 | 표시 |
| `pageF` | **약사여래 안거 전용 소개 화면** (팀 폼 원문 5단락 + 참가 안내('모집 요강'에서 개칭) + 세부일정 상시펼침 + 보시금 + CTA) | 표시 (수행 신청 활성) |
| `pageProg` | **(08-09 추가) G·C·D·E 공용 소개 화면** — pageF 동일 포맷을 JS(`showProgIntro(code)`)가 CONFIG의 `introSecs`/`reqRows`로 렌더 (히어로 문구 `hero` 필드는 사용자 지시로 삭제 — 수행명만 표시). 카드 '자세히 보기'가 여기로 옴. dsBlock은 열 때마다 `#dsStash`로 대피 후 이동(innerHTML 파괴 방지). 하단 버튼 2개: [신청하러 가기→](해당 수행 자동 체크+신청서로 스크롤) + [다른 수행 보기](`progIntroBack()` — 수행 선택 목록으로) | 표시 (수행 신청 활성) |
| `main` | 신청 화면: 초대장 + **수행 선택(1)** + **신청서 작성(2)** + 참가비(3) + 동의(4) | 표시 |

**신청 흐름(08-10 새벽 확정)** — 어르신 혼란 대응으로 여러 번 개편한 결과:
- 카드 "이 수행을 신청합니다" 체크(또는 소개 화면 CTA) → **즉시 신청서 작성(2번)으로 자동 스크롤**(`scrollToApplyForm()`, 해제 시엔 이동 없음). "아래로 내려가 주세요" 안내문·화살표(scrollCue)는 삭제됨.
- 2번 카드 내부 순서: **① 선택 수행 칩 배너**(`#chosenList` — 남색 배너+흰 알약 칩, 실시간 갱신) → **② 수행별 신청서**(`#subforms`) → **③ 신청자 기본 정보(공통)**. 마법사(단계형) 안은 논의 끝에 폐기.
- **수행별 신청서는 최근 선택이 맨 위**: `selOrder` 배열(unshift)로 순서 추적 → 방금 체크한 수행을 바로 쓰게 됨. 재렌더 시 기존 답변은 질문 라벨 기준으로 보존(`collectSubforms`).
- 신청서 머리는 팀 컬러 미니 헤더(뱃지+수행명)만 — 일자·시간·장소 미니카드와 안내문은 중복이라 삭제됨.
| `done` | 완료 화면 | 숨김 |

핵심 구현 (모두 `index.html` 하단 스크립트):
- `VIEW_IDS` / `TAB_OF` / `showView()` — **`pageF`와 `main`이 둘 다 `tab-apply`를 가리키므로**, 탭 활성 표시는 뷰가 아니라 탭 기준으로 계산함. 뷰를 추가하면 두 배열 다 갱신할 것.
- **섹션 재배치 패턴**: 혜봉·쉐랍·수행사전 → `#dictBody`, 일정표 → `#schedPageBody`, `dsDetail`(안거 세부일정) → `#introSchedule`(open 고정)로 **부팅 시 JS가 DOM을 이동**시킴. HTML 소스 위치와 실제 화면 위치가 다르니 주의.
- `DS_MAP = { G: 'ds1', C: 'ds2', D: 'ds4' }` — **F는 제거됨** (F 세부일정은 pageF에 상시 펼침, F 카드엔 '일정 자세히 보기' 없음).
- 메뉴: 모바일(≤700px) 하단 고정 탭 4개(홈·스승님 소개/수행 사전·일정표·수행 신청), PC는 상단 바(중앙 정렬+고정 간격). 라벨 '스승님 소개/수행 사전'은 모바일 두 줄(`.tabBr`)/PC 한 줄(`.tabSlash`).
- pageF CTA(`#introApplyBtn`) → `showView('main')` + F 자동 체크 + F 카드로 스크롤.

---

## 3. 수정 방법

### 페이지 수정 (⚠️ 예전 문서와 다름)
```bash
# D:\클로드\sherab-apply\index.html 을 직접 수정한 뒤:
Set-Location "D:\클로드\sherab-apply"; git add index.html; git commit -m "수정내용"; git push
```
- 반영 12~72초. 확인은 `Invoke-WebRequest`로 특정 문자열 폴링.
- **`sherab-retreat\index.html`에서 복사해오지 말 것** — 그쪽이 구버전(62KB, 08-07)이라 라이브가 과거로 덮입니다. (예전 인수인계서의 "경로 B"는 폐기)
- 브라우저 캐시: 사용자에겐 `?v=아무값` 붙인 링크 안내. 실참가자는 첫 방문이라 무관.
- 로컬 미리보기: 브라우저 패널에서 `sherab-apply` 서버(8734) 실행 → JS로 DOM 검증. (스크린샷은 패널이 화면에 떠 있어야 찍힘. Playwright MCP는 뷰포트가 안 먹는 경우 요소 단위 캡처로 우회)

### 시트에서 수정 — 화면에는 반영 안 됨
시트는 접수 저장 전용. 단, **프로그램 탭의 참가비는 백엔드가 '참가비(자동)' 기록에 사용**하므로 가격 변경 시 **CONFIG_DATA와 시트 D열 둘 다** 고칠 것 (08-09에 F를 38만으로 양쪽 동기화함).
- 시트 셀 자동 입력 시 한글 첫 어절 씹힘 → `Set-Clipboard` 후 Ctrl+V (실제로 또 발생해서 이 방법으로 복구함).
- 셀 이동은 이름 상자(name box)에 `D7` 입력 방식이 확실함 (좌표 클릭은 스케일 어긋남).

### Code.gs(백엔드) 수정 시
1. 시트 → 확장 프로그램 → Apps Script → 수정 → 저장
2. **배포 → 배포 관리 → 연필 → 새 버전** ("새 배포"는 URL이 바뀌어 링크가 죽음)

---

## 4. 콘텐츠 현황 (2026-08-09 기준)

### 프로그램 (CONFIG_DATA.programs)
| 코드 | 수행명 | 기관 | 참가비 | 비고 |
|---|---|---|---|---|
| G | 룽즁쥭 쌍악쑤 닥빠 | 행복수업협동조합 | 자율보시 | 계좌 확정(08-09): 카카오뱅크 3333-02-2894011 (서문희) · **08-25 신청 마감 처리(`closed: true`)** |
| C | 꿈요가 | 행복수업협동조합 | 180,000 | 계좌 동일(카카오뱅크·서문희) · 문의 010-6734-5597 추가 |
| D | 5일 수행 | 아나빠나사띠명상요가 | 400,000 (부분 9만/일) | 문의 010-4709-9918 추가 · **08-27 신청 마감 처리(`closed: true`)** |
| E | 한의사 茶談 수행 | 아나빠나사띠명상요가 | 자율보시 | 신청서: 진행팀 한마디 1문항 · **08-27 신청 마감 처리(`closed: true`)** |
| F | **약사여래 안거 수행** | 혜봉추모기념사업회 | **380,000** | 아래 상세 |

- **(08-09 저녁) 카드 단순화**: 카드 = 팀컬러 헤더(뱃지+수행명+한 줄 설명) + 일자/시간/장소/비용 4행 + '자세히 보기'(소개 화면 이동). 단체별 컬러 3종(행복수업 `#E8EFFE` · 아나빠나 `#E2F1F9` · 혜봉추모 `#DBE7FA`) — **전부 파란 계열이어야 함(보라 금지)·어느 하나 튀면 안 됨(사용자 지시)**. 카드 헤더·수행별 신청서 공용(`ORG_CLASS`).
- G·C·D·E 소개 화면 콘텐츠는 CONFIG의 `introSecs`(소개 섹션)·`reqRows`(참가 안내 행)가 정본. 茶談 소개문은 팀 원문, G/C/D 소개문은 수행 사전 기반 재구성 — **팀 원문 오면 교체 여지 있음**. 히어로에 문구를 지어 넣지 말 것(08-10 사용자가 전부 삭제시킴 — 수행명만).
- 참가 안내(구 '모집 요강' — 공통 개칭) 표기: 일시·시간·장소·대상·(형태)·참가비·입금·(점심·숙박·준비물·통역)·문의 순. **D(5일 수행)는 숙박 항목 없음(신청서 질문·참가 안내 모두 삭제, 08-10)**. D 시간은 카드 기준 9:00~18:00으로 통일(팀 원문 17:30과 다름 — 사용자 지시).
- **참가비 산출 내역(08-10, `64b5e84`)**: 금액이 기본 참가비와 달라지면 `feeBreakdown(p)`가 근거 줄을 참가비 표·완료 화면에 표시 — D "부분 참가 n일 × 90,000원" / F "참가비 380,000원 + 법구 구입 · 품목 +금액". `beopguInfo()`가 {금액, 품목명} 반환.
- **참가 동기 문항 삭제(08-09)**: UI·검증에서 제거, payload는 `motivation: ''`로 시트 열 유지 — Code.gs 수정 불필요.
- **(08-25) 수행 마감 메커니즘**: CONFIG_DATA 프로그램에 `"closed": true`를 넣으면 그 수행은 ① 카드 회색 처리 + '신청 마감' 뱃지 + 체크박스 대신 "신청이 마감되었습니다" 표시 ② `toggle()` 가드로 어떤 경로(카드·소개 화면 CTA·`progIntroApply`)로도 선택 불가 ③ 소개 화면(pageProg) CTA가 비활성 버튼으로 교체. **다른 수행 마감도 `closed: true` 한 줄이면 됨** (F는 pageF CTA가 별도라 마감 시 `#introApplyBtn` 처리 추가 필요). 프런트 차단만이며 Code.gs는 미수정 — 직접 POST는 여전히 저장됨(정상 사용자는 불가능한 경로).
- 문의 전화(08-10 갱신): G 010-6685-6957 / C **010-6734-5597** · 010-6685-6957 / D **010-4709-9918** · 010-6313-9189 / E 010-6313-9189. C 카드 장소 데이터에서 '1층' 삭제(실제 진행은 지하 큰명상홀 — 참가 안내에 표기).
- F 입금자명 안내의 별표 목록은 `*` 뒤가 **줄바꿈 없는 공백(U+00A0)** — 일반 공백으로 되돌리면 모바일에서 별표만 남는 줄이 다시 생김.

### F(약사여래 안거) — 08-09 집중 반영분
- 참가비 380,000 (페이지 + 시트 프로그램 탭 D7 동기화, API 검증 완료)
- **계좌 농협 302-1813-2302-91 (박서현) — 참가비·보시금 공용, 2302가 정본** (팀 폼의 2303은 오기라고 사용자 확인. 시트 J7에도 입력함)
- 문의: **김도희** 010-3007-6631 (저녁 7~9시) · **김향진** 010-5557-4597 (문자)
- 신청서 질문: 유무 → 참가 형태 → 부분참가 시간 → 숙박 → **법구 구입 희망(20세트 한정, 4종+구입하지 않음)** → 입금자명 안내(안내) → 보시금 안내(안내, html:true) → 진행팀 한마디
- **(08-10 오후 변경) 법구 옵션 3종으로 개편**: 벨과바즈라 / 다마루 / 벨과바즈라+다마루 / 구입하지 않음 — **금액은 별도 문의**(옵션에서 가격 괄호 제거). `beopguFee()`는 옵션 텍스트의 괄호 숫자를 파싱하는 로직이라 현재는 합산 0원으로 자연 비활성 — 나중에 금액을 되살리려면 옵션에 "(50,000)" 형태로 다시 넣으면 자동 합산이 살아남.
- `pageF` 소개 화면: 팀 구글폼 1페이지 원문 그대로 (혜봉스승님·닥터 쉐랍 등 **팀 표기 유지, 사이트 표기 관례와 다름** — 통일 여부는 사용자 미결정)

### 전 팀 공통
- 신청서 마지막에 "**안거 진행팀에게** 전하고 싶은 이야기…" 자유 서술 (5팀 모두)
- 카드 헤더: **뱃지형** — 기관명 알약 뱃지 + 수행명 크게(1.34rem). 선택 시 헤더 남색 반전 + **카드 안쪽 전체 연블루 채움**
- '안내' 타입 질문에 `html:true` 옵션 존재 — CONFIG_DATA에 우리가 쓴 안내문 전용. **시트 등 외부 값에는 절대 켜지 말 것** (XSS)
- 모바일 전용 줄바꿈 패턴: `<br class="mbr">`(모바일만 개행) + `<span class="pcSep">`(PC만 표시) — 보시금 안내에 사용 중, 재사용 가능

### 디자인 토큰
- 네이비 `#04194A` / 미색 `#FBF8F2` / 연블루 `#E8EFFE` / 테두리 `#D5DEEF` / Pretendard
- 허브: 제목은 박스 없는 텍스트 (배너로 오해 방지), 카드 캡션 "수행 이해를 돕기 위한"

---

## 5. ⚠️ 미완료 / 인계 사항

### 5-1. Code.gs — 정본은 편집기, 로컬 파일은 구본 (🔴 통붙이기 금지)
- **보안 패치는 2026-08-08 저녁 배포 완료(v5)** — 08-09에 라이브 검증함: 허니팟·동의값 없는 POST가 `"신청 페이지가 이전 버전입니다…"`로 거부됨.
- 패치는 Apps Script **편집기 안에서 Monaco로 병합**된 상태라, 로컬 `D:\클로드\sherab-retreat\apps-script\Code.gs`(30KB)는 **패치 이전 구본**. 편집기 정본에는 보안 패치 + 텔레그램 알림 블록(~34KB)까지 있음. **로컬 파일을 편집기에 전체 붙여넣기 하면 전부 유실됨** — Code.gs 수정은 반드시 편집기 안에서 부분 수정으로.
- 편집기 정본을 로컬에 백업해 두는 것 권장 (비공개 위치 — 이 저장소는 public이라 `.gitignore`로 제외 중).
- 남은 백엔드 잔여 과제: ① 텔레그램 수정 알림 트리거 설치 미완(봇 토큰 발급 등 사용자 절차 대기) ② 법구 구입 금액을 '참가비(자동)'에 합산하려면 doPost 수정 필요(현재는 신청서 응답으로만 기록).

### 5-2. 신청 UX 개편 — ✅ 해소됨 (08-10)
- 시안 3종(마법사·체크리스트·접기) 논의는 **폐기**. 대신 "체크 즉시 신청서로 + 칩 배너 + 수행별 신청서 우선(최근 선택 맨 위) + 공통 정보 뒤로" 구조로 확정 — 2-2장 신청 흐름 참고. 순서를 어느 쪽으로 뒤집어도 어색하다는 결론이라 마법사 재론은 비추천.

### 5-3. 사용자 액션 필요
- 사업장 시트를 각 담당자에게 **이메일 지정** 편집자 공유 (링크 공유 금지 — 명단 노출)
- ~~G·C 입금계좌 확정 시 CONFIG_DATA 수정~~ → **완료(08-09): 카카오뱅크 3333-02-2894011 (서문희)**. **시트 프로그램 탭 J4(C)·J8(G)도 입력 완료(08-10, config API로 검증)**
- ~~시트 J6(E 한의사 茶談) "계좌 추후 안내" ↔ 페이지 국민은행(김정수) 불일치~~ → **완료(08-10 오후): 사용자 확인 후 시트 J6에 국민은행 084-21-0710-611 (김정수) 입력, gviz CSV로 검증**
- 테스트 데이터 3건(테스트/테스트둘/테스트셋) 삭제 여부

### 5-4. 소소한 미결
- 팀 원문 표기(혜봉스승님·닥터 쉐랍)와 사이트 표기(혜봉 선생님·닥터 제거) 불일치 — 통일 여부 미결정
- D/E팀(아나빠나사띠)도 pageF 같은 전용 소개 화면을 원할 수 있음 (구조는 재사용 가능)

---

## 6. 알려진 함정

1. **`appendRow` 금지** — 사업장 시트 입금확인 체크박스가 1000행까지 깔려 있음. A열 첫 빈 행 찾아 `setValues`.
2. `applyContentUpdates()` 함수는 작동 안 함 — 신뢰하지 말 것. 시트 값은 손으로.
3. `연결확인` 탭 IMPORTRANGE는 일부러 IFERROR 미적용 (감싸면 "액세스 허용" 버튼이 안 뜸).
4. 개별내역(IMPORTRANGE)은 수 분 지연 가능. 접수원본에는 즉시 기록됨.
5. 시트 한글 자동 입력 시 첫 어절 누락 → 클립보드 붙여넣기 (3장 참고).
6. CSP 때문에 새 CDN/폰트는 `index.html` 상단 meta에 도메인 추가 필요. 이미지는 `https:` 전체 허용.
7. Pretendard CDN 버전 올리면 `integrity` 해시도 교체.
8. **`render()`/`renderPrograms()`는 1회 실행 전제** — 재호출하면 카드 안으로 옮긴 dsBlock이 파괴됨.
9. **일정표 구절 span** — 데스크톱은 nowrap(의미 단위 줄바꿈), 모바일(≤640px)은 normal로 해제(안 하면 화면 밖으로 삐져나감. 08-09에 실제 발생·수정).
10. 탭/뷰 추가 시 `VIEW_IDS`·`TAB_OF` 둘 다 갱신, 탭 활성 계산은 탭 기준(2-2 참고).
11. **dsBlock(세부 일정)은 단일 DOM 인스턴스** — pageProg가 열 때마다 `#dsStash`로 대피시킨 뒤 이동한다. `#progIntroBody`를 innerHTML로 다시 그리기 전에 대피 로직을 지우면 블록이 영구 파괴됨.
12. `selOrder`(선택 순서)와 `selected`(선택 여부)는 `toggle()`에서만 함께 갱신 — 다른 곳에서 `selected`만 만지면 신청서 순서가 어긋남.
13. PowerShell 5.1에서 git 커밋 메시지에 **큰따옴표(")가 들어가면 인자가 쪼개져 커밋 실패** — 메시지에서 따옴표를 빼거나 홑낫표 등으로 대체할 것 (08-10 실제 발생).
14. 로컬 미리보기 포트 8734가 다른 세션에 점유돼 있으면 `sherab-apply-b`(8632) 등 예비 서버 사용. 미리보기 패널은 사용자가 같이 조작하는 일이 있음 — 상태 검증은 스크린샷보다 JS로.

---

## 7. 작업 이력 (요약)

- **08-05**: 최초 제작, GitHub Pages 이관(브라우저 호환), 디자인 개편, E2E
- **08-08~09 오전**: og 태그, 수행 선택 재구성(G 분리·C 개칭), **CONFIG_DATA 정본화(`eb8dbaf`)**, 휴대폰 010 고정, 일정표 텍스트화, 카드 남색 배너, 세부일정 카드 이동
- **08-09 오후 (이번 세션, `7efcc44`~)**:
  - F: 수행명/기관 분리·참가비 38만(시트 동기화)·법구 구입(합산 포함)·입금자명/보시금 안내·문의 교체 → 계좌 2302 통일(시트 J7 포함) → 담당자 이름 표기
  - 전 팀 '안거 진행팀에게' 질문 / 보시금 안내 모바일 줄바꿈 / 일정표 모바일 오버플로 수정
  - **허브 구조 대개편**(`e20cc9c`): 게이트→허브→3화면 + 하단 탭(모바일)/상단 바(PC), 워딩·여백·정렬 다듬기 다수
  - **카드 뱃지형 헤더 + 선택 시 채움 + 글씨 확대**(`358b7c1`)
  - **pageF 약사여래 안거 소개 화면**(`80641ee`): 팀 폼 원문 5단락+모집요강+세부일정+보시금+CTA(F 자동선택)
- **08-09 밤 ~ 08-10 새벽 (`f27198b`~`64b5e84`, 전부 라이브 검증)**:
  - **카드 단순화**(`f27198b`): 헤더(뱃지·수행명·설명)+4행 요약, 단체별 파란 컬러 3종, '모집 요강'→'참가 안내'
  - **pageProg 공용 소개 화면**: G·C·D·E도 pageF 포맷 (introSecs·reqRows 정본), 행복수업 계좌 확정(카카오뱅크 3333-02-2894011 서문희), C·D 문의 갱신, 참가 동기 삭제
  - **신청서 작성 카드 통합**(`32e8979`) → 칩 배너 강조(`6fee5e4`) → **체크 즉시 신청서로 + scrollCue 삭제**(`1118cf3`) → D 숙박 삭제(`93c723e`) → **수행별 신청서 앞으로 + 최근 선택 우선 + CTA 직행**(`001e76f`)
  - 히어로 작성 문구 4종 삭제(`0d862f6`), 신청서 중복 블록 삭제+별표 NBSP 수정+전체 QA(`3141200`)
  - **참가비 산출 내역 + [다른 수행 보기] 버튼**(`64b5e84`)
- **08-27**: **D(5일 수행)·E(한의사 茶談) 신청 마감**(`d014259`) — `closed: true` 플래그 사용, 라이브 DOM 검증 완료(prog-G·E·D 3건 closed)
- **08-27**: **신청 현황 대시보드 신설**(`a969e0e`) — `/dashboard.html` (일정 보드·매트릭스·명단·신청 접수 시각). **주의: 신청자 실명이 공개 URL에 노출됨** — noindex는 걸어뒀으나 링크를 아는 사람은 누구나 열람 가능, 외부 공유 금지. 데이터 파이프라인: 관리 시트 → `tools/gen-dashboard-data.js`(브라우저 실행, 헤더 주석에 절차) → 클립보드 → `dd-data.json` → `node tools/build-dashboard.js` → `dashboard.html`(정본)·`dashboard-artifact.html`(Claude 아티팩트용 래퍼 제거본). 확정 제외 규칙(테스트·중복·권양희 등)은 생성기 안에 하드코딩.
- **08-25**: **G(룽즁쥭) 신청 마감**(`503d4d2`) — `closed` 플래그 메커니즘 신설(4장 참고), 라이브 DOM 검증 완료. 이 컴퓨터(C:\Users\sihac) push는 저장소 로컬 `credential.useHttpPath false`로 해결(전역 useHttpPath=true는 세스코 계정용이라 유지)

---

## 8. 작업 스타일 메모

- 한국어 캐주얼 반말체. **스크린샷에 빨간 펜** 표시로 수정 요청. 작업 중에도 추가 요청이 계속 옴 → 목록화.
- 워딩·디자인 변경은 **커밋 전에 시안으로 컨펌** 받는 걸 선호 (show_widget 목업 → 컨펌 → 구현 → 라이브 검증 순서가 잘 먹힘).
- 배포 후 실제 페이지 검증(DOM/스크린샷)을 보여주면 좋아함. 캐시 안내(`?v=`)는 매번 필요.
- "얼굴은 절대 바꾸지 마" — 이미지 생성형 편집 금지, rembg 배경 제거만.
